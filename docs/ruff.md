```uv run ruff check .``` - проверяет код и показывает ошибки
```uv run ruff check . --fix``` - пытается автоматически исправить то что безопасно
```uv run ruff format .``` - форматит код примерно как Black